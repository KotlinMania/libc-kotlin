// port-lint: source unix/linux_like/emscripten/lfs64.rs
package io.github.kotlinmania.libc.unix.linuxlike.emscripten

/**
 * Re-export wiring for `unix/linux_like/emscripten/lfs64.rs` (no definitions of its own).
 *
 * Re-exports (resolved by callers in the wiring pass):
 * - crate::{ open as open64, openat as openat64, }
 */
