// port-lint: source libc/src/new/common/linux_like/mod.rs
package io.github.kotlinmania.libc.new.common.linuxlike

/**
 * Module wiring for `new/common/linux_like` (no definitions of its own; selects and re-exports submodules).
 *
 * API that primarily comes from Linux but is also used other platforms (e.g. Android).
 *
 * Submodules: pthread.
 */
