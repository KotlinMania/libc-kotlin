// port-lint: source new/fuchsia/unistd.rs
package io.github.kotlinmania.libc.new.fuchsia

/**
 * Re-export wiring for `new/fuchsia/unistd.rs` (no definitions of its own).
 *
 * Header: `unistd.h`
 *
 * Re-exports (resolved by callers in the wiring pass):
 * - crate::new::common::posix::unistd::{ STDERR_FILENO, STDIN_FILENO, STDOUT_FILENO, }
 */
