// port-lint: source libc/src/new/bionic_libc/unistd.rs
package io.github.kotlinmania.libc.new.bioniclibc

/**
 * Re-export wiring for `new/bionic_libc/unistd.rs` (no definitions of its own).
 *
 * Header: `unistd.h`
 *
 * Re-exports (resolved by callers in the wiring pass):
 * - crate::new::common::posix::unistd::{ STDERR_FILENO, STDIN_FILENO, STDOUT_FILENO, }
 */
