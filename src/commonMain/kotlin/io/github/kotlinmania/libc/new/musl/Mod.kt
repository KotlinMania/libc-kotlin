// port-lint: source libc/src/new/musl/mod.rs
package io.github.kotlinmania.libc.new.musl

/**
 * Module wiring for `new/musl` (no definitions of its own; selects and re-exports submodules).
 *
 * Musl libc.
 *
 * * Headers: <https://git.musl-libc.org/cgit/musl> (official)
 * * Headers: <https://github.com/kraj/musl> (mirror)
 *
 * Submodules: arch, bits, pthread, sched, socket, sys, unistd.
 */
