# Port report — `libc-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `libc`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 249 `use` sites from 12 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `libc` | `libc` | 249 | 0 | COD-75 | 3 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (249 edges, 14 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `socket2-kotlin` | `socket2` | 79 |  |
| `openssl-sys-kotlin` | `openssl-sys` | 67 |  |
| `tokio-kotlin` | `tokio` | 29 |  |
| `dns-lookup-kotlin` | `dns-lookup` | 16 |  |
| `filedescriptor-kotlin` | `filedescriptor` | 14 |  |
| `crossterm-kotlin` | `crossterm` | 10 |  |
| `landlock-kotlin` | `landlock` | 9 |  |
| `seccompiler-kotlin` | `seccompiler` | 8 |  |
| `codex-kotlin` | `codex-responses-api-proxy` | 6 | ✅ |
| `core-foundation-kotlin` | `core-foundation` | 3 |  |
| `portable-pty-kotlin` | `portable-pty` | 3 |  |
| `shared-library-kotlin` | `shared_library` | 3 |  |
| `codex-kotlin` | `codex-shell-escalation` | 1 | ✅ |
| `codex-kotlin` | `codex-utils-pty` | 1 | ✅ |

### Symbol-level demand (153 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `libc::c_void` | 7 | 2 | 5 | 4 |
| `libc` | 9 | 1 | 8 | 4 |
| `libc::_SC_PAGESIZE` | 1 | 1 | 0 | 1 |
| `libc::c_uint` | 1 | 1 | 0 | 1 |
| `libc::mlock` | 1 | 1 | 0 | 1 |
| `libc::read` | 1 | 1 | 0 | 1 |
| `libc::sysconf` | 1 | 1 | 0 | 1 |
| `libc::*` | 63 | 0 | 63 | 1 |
| `libc::c_char` | 4 | 0 | 4 | 2 |
| `libc::c_int` | 4 | 0 | 4 | 3 |
| `libc::SOL_SOCKET` | 3 | 0 | 3 | 2 |
| `libc::getsockopt` | 3 | 0 | 3 | 1 |
| `libc::socklen_t` | 3 | 0 | 3 | 2 |
| `libc::POLLERR` | 2 | 0 | 2 | 1 |
| `libc::POLLIN` | 2 | 0 | 2 | 1 |
| `libc::POLLOUT` | 2 | 0 | 2 | 1 |
| `libc::SOCK_STREAM` | 2 | 0 | 2 | 2 |
| `libc::getpeereid` | 2 | 0 | 2 | 1 |
| `libc::size_t` | 2 | 0 | 2 | 2 |
| `libc::syscall` | 2 | 0 | 2 | 2 |
| `libc::winsize` | 2 | 0 | 2 | 2 |
| `libc::AF_INET` | 1 | 0 | 1 | 1 |
| `libc::AF_INET6` | 1 | 0 | 1 | 1 |
| `libc::AF_UNIX` | 1 | 0 | 1 | 1 |
| `libc::EAI_SYSTEM` | 1 | 0 | 1 | 1 |
| `libc::ENOSYS` | 1 | 0 | 1 | 1 |
| `libc::FD_ISSET` | 1 | 0 | 1 | 1 |
| `libc::FD_SET` | 1 | 0 | 1 | 1 |
| `libc::FD_SETSIZE` | 1 | 0 | 1 | 1 |
| `libc::FD_ZERO` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_DCCP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_DIVERT` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_ICMP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_ICMPV6` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_IP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_IPV6` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_MPTCP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_SCTP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_TCP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_UDP` | 1 | 0 | 1 | 1 |
| `libc::IPPROTO_UDPLITE` | 1 | 0 | 1 | 1 |
| `libc::IPV6_ADD_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IPV6_DROP_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IPV6_HDRINCL` | 1 | 0 | 1 | 1 |
| `libc::IPV6_JOIN_GROUP` | 1 | 0 | 1 | 1 |
| `libc::IPV6_LEAVE_GROUP` | 1 | 0 | 1 | 1 |
| `libc::IPV6_MULTICAST_HOPS` | 1 | 0 | 1 | 1 |
| `libc::IPV6_MULTICAST_IF` | 1 | 0 | 1 | 1 |
| `libc::IPV6_MULTICAST_LOOP` | 1 | 0 | 1 | 1 |
| `libc::IPV6_RECVHOPLIMIT` | 1 | 0 | 1 | 1 |
| `libc::IPV6_RECVTCLASS` | 1 | 0 | 1 | 1 |
| `libc::IPV6_UNICAST_HOPS` | 1 | 0 | 1 | 1 |
| `libc::IPV6_V6ONLY` | 1 | 0 | 1 | 1 |
| `libc::IP_ADD_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IP_ADD_SOURCE_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IP_DROP_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IP_DROP_SOURCE_MEMBERSHIP` | 1 | 0 | 1 | 1 |
| `libc::IP_HDRINCL` | 1 | 0 | 1 | 1 |
| `libc::IP_MULTICAST_IF` | 1 | 0 | 1 | 1 |
| `libc::IP_MULTICAST_LOOP` | 1 | 0 | 1 | 1 |
| `libc::IP_MULTICAST_TTL` | 1 | 0 | 1 | 1 |
| `libc::IP_RECVTOS` | 1 | 0 | 1 | 1 |
| `libc::IP_TOS` | 1 | 0 | 1 | 1 |
| `libc::IP_TTL` | 1 | 0 | 1 | 1 |
| `libc::LOCAL_PEEREID` | 1 | 0 | 1 | 1 |
| `libc::LOCAL_PEEREPID` | 1 | 0 | 1 | 1 |
| `libc::MSG_OOB` | 1 | 0 | 1 | 1 |
| `libc::MSG_PEEK` | 1 | 0 | 1 | 1 |
| `libc::MSG_TRUNC` | 1 | 0 | 1 | 1 |
| `libc::NI_NAMEREQD` | 1 | 0 | 1 | 1 |
| `libc::NI_NUMERICSERV` | 1 | 0 | 1 | 1 |
| `libc::O_RDWR` | 1 | 0 | 1 | 1 |
| `libc::PATH_MAX` | 1 | 0 | 1 | 1 |
| `libc::PIDFD_NONBLOCK` | 1 | 0 | 1 | 1 |
| `libc::POLLHUP` | 1 | 0 | 1 | 1 |
| `libc::SECCOMP_RET_ALLOW` | 1 | 0 | 1 | 1 |
| `libc::SECCOMP_RET_DATA` | 1 | 0 | 1 | 1 |
| `libc::SECCOMP_RET_ERRNO` | 1 | 0 | 1 | 1 |
| `libc::SECCOMP_RET_KILL_PROCESS` | 1 | 0 | 1 | 1 |
| `libc::SECCOMP_RET_KILL_THREAD` | 1 | 0 | 1 | 1 |

_… and 73 more symbols (full list in `workspace_dep_graph.json`)._

#### Per-symbol detail (top 120 symbols)

##### `libc::c_void` (7 sites, 2 codex, 5 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:42` — `use libc::c_void`
- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:167` — `use libc::c_void`

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::c_void`
- `openssl-sys-kotlin/openssl-sys` `src/lib.rs:46` — `use libc::c_void`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:78` — `use libc::c_void`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:136` — `use libc::c_void`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:215` — `use libc::c_void`

##### `libc` (9 sites, 1 codex, 8 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/pipe.rs:25` — `use libc`

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/err.rs:106` — `use libc as c`
- `dns-lookup-kotlin/dns-lookup` `src/lookup.rs:62` — `use libc`
- `dns-lookup-kotlin/dns-lookup` `src/types.rs:2` — `use libc as c`
- `portable-pty-kotlin/portable-pty` `src/lib.rs:43` — `use libc`
- `portable-pty-kotlin/portable-pty` `src/unix.rs:6` — `use libc`
- `shared-library-kotlin/shared_library` `src/dynamic_library.rs:21` — `use libc`
- `shared-library-kotlin/shared_library` `src/dynamic_library.rs:230` — `use libc`
- `shared-library-kotlin/shared_library` `src/dynamic_library.rs:312` — `use libc`

##### `libc::_SC_PAGESIZE` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:166` — `use libc::_SC_PAGESIZE`

##### `libc::c_uint` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:1` — `use libc::c_uint`

##### `libc::mlock` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:168` — `use libc::mlock`

##### `libc::read` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:43` — `use libc::read`

##### `libc::sysconf` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:169` — `use libc::sysconf`

##### `libc::*` (63 sites, 0 codex, 63 other, 1 caller crates)

Other workspace call sites:

- `openssl-sys-kotlin/openssl-sys` `src/aes.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/asn1.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/bio.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/bn.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/cms.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/core_dispatch.rs:2` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/crypto.rs:2` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/dh.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/dsa.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/dtls1.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/ec.rs:1` — `use libc::*`
- `openssl-sys-kotlin/openssl-sys` `src/err.rs:1` — `use libc::*`

_… 51 more call sites._

##### `libc::c_char` (4 sites, 0 codex, 4 other, 2 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/addrinfo.rs:11` — `use libc::c_char as libc_c_char`
- `dns-lookup-kotlin/dns-lookup` `src/hostname.rs:7` — `use libc::c_char as libc_c_char`
- `dns-lookup-kotlin/dns-lookup` `src/nameinfo.rs:9` — `use libc::c_char as libc_c_char`
- `openssl-sys-kotlin/openssl-sys` `src/lib.rs:46` — `use libc::c_char`

##### `libc::c_int` (4 sites, 0 codex, 4 other, 3 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/errors.rs:4` — `use libc::c_int`
- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::c_int`
- `openssl-sys-kotlin/openssl-sys` `src/lib.rs:13` — `pub_use libc::c_int`
- `tokio-kotlin/tokio` `tests/signal_realtime.rs:9` — `use libc::c_int`

##### `libc::SOL_SOCKET` (3 sites, 0 codex, 3 other, 2 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SOL_SOCKET`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:78` — `use libc::SOL_SOCKET`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:136` — `use libc::SOL_SOCKET`

##### `libc::getsockopt` (3 sites, 0 codex, 3 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:78` — `use libc::getsockopt`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:136` — `use libc::getsockopt`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:215` — `use libc::getsockopt`

##### `libc::socklen_t` (3 sites, 0 codex, 3 other, 2 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:104` — `pub_use libc::socklen_t`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:78` — `use libc::socklen_t`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:136` — `use libc::socklen_t`

##### `libc::POLLERR` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:440` — `pub_use libc::POLLERR`
- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::POLLERR`

##### `libc::POLLIN` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:440` — `pub_use libc::POLLIN`
- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::POLLIN`

##### `libc::POLLOUT` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:440` — `pub_use libc::POLLOUT`
- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::POLLOUT`

##### `libc::SOCK_STREAM` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/lookup.rs:6` — `use libc::SOCK_STREAM`
- `socket2-kotlin/socket2` `src/sys/unix.rs:82` — `pub_use libc::SOCK_STREAM`

##### `libc::getpeereid` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:178` — `use libc::getpeereid`
- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:215` — `use libc::getpeereid`

##### `libc::size_t` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/file_descriptor.rs:4` — `use libc::size_t`
- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::size_t`

##### `libc::syscall` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::syscall`
- `tokio-kotlin/tokio` `src/process/unix/pidfd_reaper.rs:9` — `use libc::syscall`

##### `libc::winsize` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/unix.rs:10` — `use libc::winsize`
- `portable-pty-kotlin/portable-pty` `src/unix.rs:6` — `use libc::winsize`

##### `libc::AF_INET` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:72` — `pub_use libc::AF_INET`

##### `libc::AF_INET6` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:72` — `pub_use libc::AF_INET6`

##### `libc::AF_UNIX` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:72` — `pub_use libc::AF_UNIX`

##### `libc::EAI_SYSTEM` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/err.rs:182` — `use libc::EAI_SYSTEM`

##### `libc::ENOSYS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/process/unix/pidfd_reaper.rs:9` — `use libc::ENOSYS`

##### `libc::FD_ISSET` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::FD_ISSET`

##### `libc::FD_SET` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::FD_SET`

##### `libc::FD_SETSIZE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::FD_SETSIZE`

##### `libc::FD_ZERO` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::FD_ZERO`

##### `libc::IPPROTO_DCCP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:85` — `pub_use libc::IPPROTO_DCCP`

##### `libc::IPPROTO_DIVERT` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:103` — `pub_use libc::IPPROTO_DIVERT`

##### `libc::IPPROTO_ICMP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:100` — `pub_use libc::IPPROTO_ICMP`

##### `libc::IPPROTO_ICMPV6` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:100` — `pub_use libc::IPPROTO_ICMPV6`

##### `libc::IPPROTO_IP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPPROTO_IP`

##### `libc::IPPROTO_IPV6` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPPROTO_IPV6`

##### `libc::IPPROTO_MPTCP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:87` — `pub_use libc::IPPROTO_MPTCP`

##### `libc::IPPROTO_SCTP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:89` — `pub_use libc::IPPROTO_SCTP`

##### `libc::IPPROTO_TCP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:100` — `pub_use libc::IPPROTO_TCP`

##### `libc::IPPROTO_UDP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:100` — `pub_use libc::IPPROTO_UDP`

##### `libc::IPPROTO_UDPLITE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:99` — `pub_use libc::IPPROTO_UDPLITE`

##### `libc::IPV6_ADD_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:236` — `pub_use libc::IPV6_ADD_MEMBERSHIP`

##### `libc::IPV6_DROP_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:236` — `pub_use libc::IPV6_DROP_MEMBERSHIP`

##### `libc::IPV6_HDRINCL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:116` — `pub_use libc::IPV6_HDRINCL`

##### `libc::IPV6_JOIN_GROUP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:251` — `pub_use libc::IPV6_JOIN_GROUP as IPV6_ADD_MEMBERSHIP`

##### `libc::IPV6_LEAVE_GROUP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:251` — `pub_use libc::IPV6_LEAVE_GROUP as IPV6_DROP_MEMBERSHIP`

##### `libc::IPV6_MULTICAST_HOPS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPV6_MULTICAST_HOPS`

##### `libc::IPV6_MULTICAST_IF` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPV6_MULTICAST_IF`

##### `libc::IPV6_MULTICAST_LOOP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPV6_MULTICAST_LOOP`

##### `libc::IPV6_RECVHOPLIMIT` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:134` — `pub_use libc::IPV6_RECVHOPLIMIT`

##### `libc::IPV6_RECVTCLASS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:148` — `pub_use libc::IPV6_RECVTCLASS`

##### `libc::IPV6_UNICAST_HOPS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPV6_UNICAST_HOPS`

##### `libc::IPV6_V6ONLY` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IPV6_V6ONLY`

##### `libc::IP_ADD_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_ADD_MEMBERSHIP`

##### `libc::IP_ADD_SOURCE_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:218` — `pub_use libc::IP_ADD_SOURCE_MEMBERSHIP`

##### `libc::IP_DROP_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_DROP_MEMBERSHIP`

##### `libc::IP_DROP_SOURCE_MEMBERSHIP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:218` — `pub_use libc::IP_DROP_SOURCE_MEMBERSHIP`

##### `libc::IP_HDRINCL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:150` — `pub_use libc::IP_HDRINCL`

##### `libc::IP_MULTICAST_IF` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_MULTICAST_IF`

##### `libc::IP_MULTICAST_LOOP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_MULTICAST_LOOP`

##### `libc::IP_MULTICAST_TTL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_MULTICAST_TTL`

##### `libc::IP_RECVTOS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:167` — `pub_use libc::IP_RECVTOS`

##### `libc::IP_TOS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:175` — `pub_use libc::IP_TOS`

##### `libc::IP_TTL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::IP_TTL`

##### `libc::LOCAL_PEEREID` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:136` — `use libc::LOCAL_PEEREID`

##### `libc::LOCAL_PEEREPID` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:215` — `use libc::LOCAL_PEEREPID`

##### `libc::MSG_OOB` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::MSG_OOB`

##### `libc::MSG_PEEK` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::MSG_PEEK`

##### `libc::MSG_TRUNC` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:109` — `pub_use libc::MSG_TRUNC`

##### `libc::NI_NAMEREQD` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/lookup.rs:6` — `use libc::NI_NAMEREQD`

##### `libc::NI_NUMERICSERV` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/lookup.rs:6` — `use libc::NI_NUMERICSERV`

##### `libc::O_RDWR` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `core-foundation-kotlin/core-foundation` `src/filedescriptor.rs:105` — `use libc::O_RDWR`

##### `libc::PATH_MAX` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `core-foundation-kotlin/core-foundation` `src/url.rs:22` — `use libc::PATH_MAX`

##### `libc::PIDFD_NONBLOCK` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/process/unix/pidfd_reaper.rs:9` — `use libc::PIDFD_NONBLOCK`

##### `libc::POLLHUP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:440` — `pub_use libc::POLLHUP`

##### `libc::SECCOMP_RET_ALLOW` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_ALLOW`

##### `libc::SECCOMP_RET_DATA` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_DATA`

##### `libc::SECCOMP_RET_ERRNO` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_ERRNO`

##### `libc::SECCOMP_RET_KILL_PROCESS` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_KILL_PROCESS`

##### `libc::SECCOMP_RET_KILL_THREAD` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_KILL_THREAD`

##### `libc::SECCOMP_RET_LOG` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_LOG`

##### `libc::SECCOMP_RET_TRACE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_TRACE`

##### `libc::SECCOMP_RET_TRAP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `seccompiler-kotlin/seccompiler` `src/backend/mod.rs:21` — `use libc::SECCOMP_RET_TRAP`

##### `libc::SOCK_DCCP` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:77` — `pub_use libc::SOCK_DCCP`

##### `libc::SOCK_DGRAM` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:82` — `pub_use libc::SOCK_DGRAM`

##### `libc::SOCK_RAW` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:79` — `pub_use libc::SOCK_RAW`

##### `libc::SOCK_SEQPACKET` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:81` — `pub_use libc::SOCK_SEQPACKET`

##### `libc::SOL_LOCAL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:215` — `use libc::SOL_LOCAL`

##### `libc::SO_BROADCAST` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_BROADCAST`

##### `libc::SO_ERROR` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_ERROR`

##### `libc::SO_KEEPALIVE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_KEEPALIVE`

##### `libc::SO_LINGER` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:183` — `pub_use libc::SO_LINGER`

##### `libc::SO_LINGER_SEC` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:191` — `pub_use libc::SO_LINGER_SEC as SO_LINGER`

##### `libc::SO_OOBINLINE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:111` — `pub_use libc::SO_OOBINLINE`

##### `libc::SO_PASSCRED` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:193` — `pub_use libc::SO_PASSCRED`

##### `libc::SO_PEERCRED` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/net/unix/ucred.rs:78` — `use libc::SO_PEERCRED`

##### `libc::SO_PRIORITY` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:198` — `pub_use libc::SO_PRIORITY`

##### `libc::SO_RCVBUF` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_RCVBUF`

##### `libc::SO_RCVTIMEO` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_RCVTIMEO`

##### `libc::SO_REUSEADDR` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_REUSEADDR`

##### `libc::SO_SNDBUF` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_SNDBUF`

##### `libc::SO_SNDTIMEO` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_SNDTIMEO`

##### `libc::SO_TYPE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::SO_TYPE`

##### `libc::STDOUT_FILENO` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/unix.rs:10` — `use libc::STDOUT_FILENO`

##### `libc::SYS_landlock_add_rule` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::SYS_landlock_add_rule`

##### `libc::SYS_landlock_create_ruleset` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::SYS_landlock_create_ruleset`

##### `libc::SYS_landlock_restrict_self` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::SYS_landlock_restrict_self`

##### `libc::SYS_pidfd_open` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tokio-kotlin/tokio` `src/process/unix/pidfd_reaper.rs:9` — `use libc::SYS_pidfd_open`

##### `libc::TCP_KEEPALIVE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:285` — `use libc::TCP_KEEPALIVE as KEEPALIVE_TIME`

##### `libc::TCP_KEEPCNT` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:272` — `pub_use libc::TCP_KEEPCNT`

##### `libc::TCP_KEEPIDLE` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:297` — `use libc::TCP_KEEPIDLE as KEEPALIVE_TIME`

##### `libc::TCP_KEEPINTVL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:272` — `pub_use libc::TCP_KEEPINTVL`

##### `libc::TCP_NODELAY` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `socket2-kotlin/socket2` `src/sys/unix.rs:199` — `pub_use libc::TCP_NODELAY`

##### `libc::TCSANOW` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/unix.rs:10` — `use libc::TCSANOW`

##### `libc::TIOCGWINSZ` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/unix.rs:10` — `use libc::TIOCGWINSZ`

##### `libc::__u32` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/uapi/mod.rs:61` — `use libc::__u32`

##### `libc::addrinfo` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/addrinfo.rs:11` — `use libc::addrinfo as c_addrinfo`

##### `libc::c_long` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `openssl-sys-kotlin/openssl-sys` `src/lib.rs:46` — `use libc::c_long`

##### `libc::cfmakeraw` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/terminal/sys/unix.rs:10` — `use libc::cfmakeraw`

##### `libc::fd_set` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `filedescriptor-kotlin/filedescriptor` `src/unix.rs:467` — `use libc::fd_set`

_… 33 more symbols. See `workspace_dep_graph.json` for the full list._

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
