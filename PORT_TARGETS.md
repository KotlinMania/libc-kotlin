# Port targets — `libc-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `libc` that codex-rs uses, and therefore the minimum subset that `libc-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-75] (rank `3`, tier `direct`, status `To Do`)

**Cargo package name**: `libc`
**Rust module name (in `use` statements)**: `libc`

## Summary

- **7** distinct symbols imported by codex-rs
- **8** total `use` statements
- **3** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `libc::c_void`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-responses-api-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `responses-api-proxy/src/read_api_key.rs:42` (`codex-responses-api-proxy`) — `use libc::c_void`
- `responses-api-proxy/src/read_api_key.rs:167` (`codex-responses-api-proxy`) — `use libc::c_void`

### `libc::c_uint`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:1` (`codex-shell-escalation`) — `use libc::c_uint`

### `libc::read`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-responses-api-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `responses-api-proxy/src/read_api_key.rs:43` (`codex-responses-api-proxy`) — `use libc::read`

### `libc::_SC_PAGESIZE`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-responses-api-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `responses-api-proxy/src/read_api_key.rs:166` (`codex-responses-api-proxy`) — `use libc::_SC_PAGESIZE`

### `libc::mlock`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-responses-api-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `responses-api-proxy/src/read_api_key.rs:168` (`codex-responses-api-proxy`) — `use libc::mlock`

### `libc::sysconf`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-responses-api-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `responses-api-proxy/src/read_api_key.rs:169` (`codex-responses-api-proxy`) — `use libc::sysconf`

### `libc`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/pipe.rs:25` (`codex-utils-pty`) — `use libc`
